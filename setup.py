from setuptools import setup

setup(
    name='dango-nestgrouplists',
    version='0.2',
    description='Generates a list for a model which is grouped by some given field and nest group members. Also include required annotations',
    author='Santiago Fulladoza',
    packages=['nestgrouplists'],
)
